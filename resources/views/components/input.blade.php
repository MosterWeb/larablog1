<div class="mb-3">
    <!-- The biggest battle is the war against ignorance. - Mustafa Kemal Atatürk -->
    <label for="{{ $id }}" class="form-label">{{ $title }}</label>
    <input name="{{ $name }}" type="{{ $type }}" class="form-control" id="{{ $id }}" placeholder="{{ $placeholder }}">
</div>